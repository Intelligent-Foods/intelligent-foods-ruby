# frozen_string_literal: true

RSpec.describe IntelligentFoods do
  it "has a version number" do
    expect(IntelligentFoods::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
